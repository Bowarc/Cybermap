[package]
name = "osm"
version = "0.1.0"
edition = "2024"

[dependencies]

[dev-dependencies]
reqwest = { version = "0.12.26", features = ["blocking"] }
time = { git = "https://github.com/bowarc/crates", package = "time" }
